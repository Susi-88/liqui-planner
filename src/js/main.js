// Variables
// Eingabedaten holen
let titel = prompt("Titel:");
    console.log(titel);
let typ = prompt("Typ (Einnahme oder Ausgabe):");
    console.log(typ);
let betrag = prompt("Betrag (in Cent):");
    console.log(betrag);
let datum = prompt("Datum (jjjj-mm-tt):");
    console.log(datum);

// Gesammtbilant
let einnahmen;
let ausgaben;
let bilanz;